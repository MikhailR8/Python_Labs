# This is a sample Python script.

# Press Shift+F10 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.


def print_hi(name):
    # Use a breakpoint in the code line below to debug your script.
    print(f'Hi, {name}')  # Press Ctrl+F8 to toggle the breakpoint.


# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    print_hi('PyCharm')

# for k in range(1, 5):
#     quat_p = integrate_quaternion_t(quat_0, k)
#     quat_p_con = quat_p.conjugate()
#     for i in range(n):
#         for j in range(n):
#             quat_l = Quaternion(0, np.array([x[i][j], y[i][j], z[i][j]]))
#             quat_out = quat_p >> quat_l >> quat_p_con
#             x[i][j], y[i][j], z[i][j] = quat_out.get_axis()
#     out = numpy.concatenate((out, [x, y, z]))
#
# print(out)
# def data_gen(framenumber, arr, plot):
#     ax.clear()
#     plot = ax.plot_surface(arr[framenumber][0],
#                            arr[framenumber][1], arr[framenumber][2])
#     return plot,
#
# pam_ani = animation.FuncAnimation(fig, data_gen, fargs=(out, plot),
#                               interval=100, blit=False, frames=9)

# The amount of frames in the animation

out = numpy.array([x, y, z])
